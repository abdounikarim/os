# CLAUDE.md

Instructions for Claude Code on this project.

## Git workflow

- For every new feature, open a new branch.
- Do the requested work on that branch.
- Make the requested commit(s).
- Never run `git push`.
