# Python toolchain — stub, nothing installed yet.
# Add formulae/casks and config here when Python tooling is needed
# (e.g. pyenv, poetry, black).

python-install: ## Install Python toolchain (stub)
		@echo "python package is a stub — nothing to install yet"

python-update: python-install ## Update Python toolchain (stub)

python-remove: ## Remove Python toolchain (stub)
		@echo "python package is a stub — nothing to remove"
